// ─── Посадка после входа ──────────────────────────────────────────────────────
//
// Владелец: «לכל משתמש אמור להיפתח ישר למסך הראשי» — каждый пользователь после
// входа попадает на ГЛАВНЫЙ экран /dashboard (сетка модулей + повестка + виджеты,
// уже отфильтрованные под его доступ). Прежняя «посадка по роли» на отдельный
// экран убрана — она путала (разные пользователи видели разные стартовые страницы).
//
// Прямые ссылки (?from=...) по-прежнему уважаются в LoginForm — это только выбор
// стартового экрана для «пустого» входа.

/**
 * Куда отправить пользователя после входа. Всегда — общий главный экран.
 */
export function landingRouteForRoles(_roles: string[] | null | undefined): string {
  return '/dashboard'
}
