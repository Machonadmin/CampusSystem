# מעקב החלטות ומשימות פתוחות (Backlog)

מסמך חי שמרכז את מה שנשאר פתוח, כדי ששום דבר לא יאבד בין סשנים.
עודכן לאחרונה: 2026-09-08. מבוסס על `PROJECT_STATUS.md` (סריקת המערכת) ועל העבודה מאז.

**עדכון 2026-09-08:** נסגרו — `CRON_SECRET` מוגדר ב-Vercel (A1); בדיקת ה-SQL הורצה,
`campus_admin`/`admin` לא קיימים בפרודקשן (A2); מוזגו ל-main: הקשחת cron (A3),
סבב QA, תיקוני P1 (PGRST205/204 deploy-safe בכל הקוד, קישור היעדרויות, שמירת מסלול בקבלה).
נותרו: החלטות המוצר (B), ובאגים קטנים חדשים לטריאז' (C5, C6).

> אין כאן שמות של אנשים — הכל לפי תפקידים, בהתאם למדיניות.

---

## A. פעולות תפעוליות ממתינות (אתה מבצע ב-Vercel / Supabase)

| # | פעולה | סטטוס | הערה |
|---|---|---|---|
| A1 | **להגדיר `CRON_SECRET` ב-Vercel** | 🔴 פתוח | חובה **לפני** מיזוג באטץ' ה-cron, אחרת עבודות הלילה (שיעורים 03:00, תזכורות 06:00) ייעצרו. Settings → Environment Variables → Production → Save → Redeploy |
| A2 | **להריץ SQL** לבדיקת `campus_admin` + `admin` בפרודקשן | 🔴 פתוח | השאילתה נשלחה בצ'אט. תוצאה ריקה = טוב |
| A3 | **למזג באטץ' cron** (`claude/cron-hardening-role-fix`) | 🟡 מאומת, ממתין ל-A1 | אחרי שתגדיר את הסוד |
| A4 | Sentry DSN | ⚪ אופציונלי | הקוד מוכן, 15 דקות עבודה |
| A5 | Staging ל-E2E (`STAGING_URL`) | ⚪ אופציונלי | כדי שהטסטים הקצה-לקצה ירוצו ב-CI |
| A6 | גיבויים — לוודא Supabase Pro + בדיקת שחזור | ⚪ להחלטה תקציבית | "גיבוי שלא נבדק אינו גיבוי" |

---

## B. החלטות מוצר שחוסמות בנייה (צריך תשובות ממך/מהלקוח)

| # | נושא | מה חסום | השאלות |
|---|---|---|---|
| B1 | **חובות אקדמיים** (הפער הכי גדול) | אין מושג "השלמת קורס" או "חוב" | (1) מה מגדיר "השלימה"? (2) מה קורה בכישלון? (3) מי סוגר חוב ואיך? (4) האם חוב חוסם מעבר-שנה/תעודה? |
| B2 | **אחראי כספים** (§6.2) | זרימת אישור הנחה 90% חסומה | מי מקבל `finance.approve_discount` + `confirm_payment`? |
| B3 | **תיק עובד עשיר** | חוזה/שכר/תעודות נשמרים כ-JSON בהערה, קובץ החוזה לא נשמר | מה צריך כקבצים? (קו"ח, דיפלומות, המלצות, חוזה חתום) |
| B4 | **בקרת גישה פיזית** | לא קיים בכלל | האם בכלל נדרש? (כרטיסים, אזורים) |
| B5 | **איחוד שני מנגנוני מסמכים** | `document_records` מול `journey_documents` | לאחד? |
| B6 | **אמהות בית / מחנכות** | אין ישות "מחנכת של תלמידה X" | איך זה עובד במכון בפועל? |

---

## C. ניצחונות טכניים שאפשר לקחת בלי החלטה (מוכנים לפרומט)

| # | פריט | ערך | הערה |
|---|---|---|---|
| C1 | **סבב QA / ציד-באגים** | גבוה | מפיק רשימת תיקונים אמיתית; לא דורש החלטות |
| C2 | **ניקוי `admin` במסלולי positions** | בינוני | רק אחרי A2 (לוודא שאף אחד לא מחזיק אותו) |
| C3 | שריון קודי-תפקיד שבתבניות תהליך | נמוך | רק superadmin יכול לגעת בתפקידים ממילא |
| C4 | `<img>` → `next/image` | נמוך | מנקה אזהרות lint; מעט סיכון לתמונות דינמיות |
| C5 | נמעני התראת-היעדרויות בלי גישה למודול לימודים → מופנים לבית | נמוך | לסנן נמענים לבעלי גישה (שינוי התנהגות) |
| C6 | דוח היחידה קורא מסלול בלי סינון `role='primary'` | נמוך | תלמידה עם מסלול נוסף עלולה להופיע עם המסלול הלא-נכון; תיקון של שורה |

---

## E. אבטחה — נסגר בסבב + מה שנותר להחלטתך

**נסגר בסבב האבטחה (ענף `claude/product-improvements-zq6cq4`, ממתין למיזוג):**
הגבלת-קצב על מסכי התחברות/שינוי-סיסמה (נגד ניחוש סיסמאות) · הסתרת פרטי-שגיאה
פנימיים מהלקוח (500) · יישור ה-middleware ל-fail-closed · חיזוק שומר-כיסוי-ההרשאות
(מתעלם מבדיקות שמופיעות רק בהערה/import) · טסט-שמירה על מצב "צפייה בלבד" בהתחזות.

**נותר להחלטתך (לא בוצע — דורש הכרעה):**

| # | נושא | השאלה | חומרה |
|---|---|---|---|
| E1 | **גישה לתיק רפואי/נפשי** | מי שמחזיק הרשאת "רופא/פסיכולוג" רואה כרגע את התיק הרפואי של **כל** תלמידה (לפי מספר בתעודת-הזהות של המסע). זו מרפאה אחת שרואה את כולן (מכוון) — או שרופא צריך לראות רק תלמידות של המחלקה/שהוקצו לו? | גבוהה (מידע רגיש) |
| E2 | **הסתרת מסמכי-זהות** | מי אמור לראות מספר-דרכון/אזרחות/מצב-משפחתי בכרטיס התלמידה? כרגע כל בעל הרשאת-צפייה רואה. לגדר לפי הרשאה (כמו בכספים)? | בינונית |
| E3 | **שדרוג Next.js** | לגרסה יש התראות-אבטחה; התיקון דורש קפיצת-גרסה גדולה (14→16) שמצריכה בדיקות זהירות. לתזמן כמשימה נפרדת? | בינונית (פרודקשן ב-Vercel ממתן חלק) |

---

## F. עיצוב — נסגר בסבב + המשך אפשרי

**נסגר (ענף `claude/product-improvements-zq6cq4`, ממתין למיזוג):**
פלטת-מודולים מודעת-ערכה (נסגרו 3 התנגשויות זהות: אוכל=תורמים, בוגרות=אנשי-קשר,
כספים=מרפאה; וכן התנגשויות מול טוקני-מצב) + טסט-הגנה שנופל על התנגשות חוזרת ·
תיקון פונט לכל השפות · דפדוף ברשימת אנשים (נחתכה ב-200) · צ׳יפים קריאים בכהה ·
סולמות רדיוס/מרווח/גופן/שכבות · רכיבי `Button`+`Badge` משותפים (גל ראשון: 8 קבצים) ·
22 עמודי-פירוט עברו ל-`ModuleHeader` המשותף.

**המשך אפשרי (לא בוצע):**

| # | פריט | ערך | הערה |
|---|---|---|---|
| F1 | אימוץ `Button`/`Badge` בשאר האפליקציה | גבוה | נותרו ~10 הגדרות-כפתור מקומיות ו-~18 גרסאות-צ׳יפ בשורה |
| F2 | החלפת מאות המספרים הקבועים בסולמות | בינוני | ~670 `borderRadius: 8` וכו׳; מכני אך רחב — עדיף בהדרגה |
| F3 | ניקוי הכחול הישן `#4BAED4` (17 מקומות) | בינוני | צבע-מותג ישן ללא טוקן וללא גרסה כהה; מופיע גם ב-`Header.tsx` |
| F4 | איחוד מפת-האייקונים הכפולה | נמוך | `app/dashboard/page.tsx` מול `Sidebar.tsx`; וגם: לחברותא אין אייקון משלה |
| F5 | רוחב-עמוד מקסימלי למסך רחב | — | **נדחה על ידך במפורש** — לא לבצע בלי אישור מחודש |
| F6 | ~6 טבלאות ללא מעטפת-גלילה | נמוך | עלולות לגלוש בטלפון |

> ⚠️ כל האימות בסבב היה **סטטי** (tsc/טסטים/lint/build). המסכים **לא נבדקו בעין** —
> ראה סעיף D: צריך מעבר ידני על מסכים בבהיר ובכהה.

---

## D. דורש בדיקת משתמש אמיתי (לא קוד — אתה/הצוות בודקים במכשיר)

- תהליך הקבלה מקצה-לקצה עם 4–5 חותמים אמיתיים.
- מודול הקודש בשטח (אחרי מיפוי התלמידות ל-8 הרמות).
- פורטל התלמידה.
- חשבון אחראית היהדות — נחיתה, סיידבר, ושכלום לא חסר (בלוק "עוד").
- ניווט "חזרה" ב-PWA המותקן על אנדרואיד.

---

## מה כבר נסגר (לתיעוד)
מודול היהדות המלא · בירור יהדות דו-שלבי · לוח שנה אקדמי · תיקון ניווט "חזרה" (שיטתי+טסט) ·
נחיתה+סיידבר ייעודיים לאחראית היהדות · הסרת שמות מקודדים מה-UI · ספירת תלמידות אמיתית ·
שכר-עובדים בתפריט · צופה יומן-שינויים · הקשחת cron · תיקון campus_admin · שריון קודי-תפקיד.
