-- Необязательная «метка яруса» для узлов учебной структуры (departments):
-- владелец помечает под-единицу как רמה / שיעור / כיתה (или любой текст), чтобы
-- дерево кодеша читалось осмысленно. Просто nullable-текст, ни на что не влияет.
--
-- Additive и nullable — деплой безопасен. RLS отключён. Применять ВРУЧНУЮ.

ALTER TABLE departments ADD COLUMN IF NOT EXISTS structure_tier TEXT;
