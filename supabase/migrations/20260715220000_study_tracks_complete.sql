-- ═════════════════════════════════════════════════════════════════════
-- Дополнение справочника учебных маршрутов (מסלולי חול) до согласованной
-- иерархии владельца: добавляем אוניברסיטה (university) и אמונה (emuna,
-- пока НЕактивна). Ранее были только touro / school / college(מכללה).
--
-- Порядок сортировки приводим к порядку иерархии (владелец 2026-07-15):
--   בית ספר → קולג'/מכללה → אוניברסיטה → טורו → אמונה(неактивна).
--
-- ВНИМАНИЕ (открыто владельцу): существующий 'college' назван 'מכללה', тогда
-- как владелец говорит 'קולג''. Переименование НЕ делаем здесь — ждём решения.
--
-- RLS отключён; доступ ограничивает API. Применять ВРУЧНУЮ. Идемпотентно.
-- ═════════════════════════════════════════════════════════════════════

INSERT INTO study_tracks (code, name_he, name_ru, name_en, sort_order, is_active) VALUES
  ('university', 'אוניברסיטה', 'Университет', 'University', 30, TRUE),
  ('emuna',      'אמונה',      'Эмуна',       'Emuna',      50, FALSE)
ON CONFLICT (code) DO NOTHING;

-- Порядок по иерархии (сами строки — существующие, меняем только sort_order).
UPDATE study_tracks SET sort_order = 10 WHERE code = 'school';
UPDATE study_tracks SET sort_order = 20 WHERE code = 'college';
UPDATE study_tracks SET sort_order = 40 WHERE code = 'touro';
