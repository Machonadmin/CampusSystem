-- ═════════════════════════════════════════════════════════════════════
-- כפיית החלפת סיסמה בכניסה ראשונה — флаг «нужно сменить пароль».
--
-- Когда учётка создаётся с ВРЕМЕННЫМ (сгенерированным) паролем — ставим флаг.
-- При первом входе интерфейс мягко, но настойчиво требует задать свой пароль
-- (решение владельца: «חסימה עם בקשה רכה» — блокирует использование, но
-- формулировка мягкая). После смены пароля флаг снимается.
--
-- Существующие пользователи не затронуты (DEFAULT false). Идемпотентно.
-- ═════════════════════════════════════════════════════════════════════

ALTER TABLE person_accounts      ADD COLUMN IF NOT EXISTS must_change_password boolean NOT NULL DEFAULT false;
ALTER TABLE student_credentials  ADD COLUMN IF NOT EXISTS must_change_password boolean NOT NULL DEFAULT false;
